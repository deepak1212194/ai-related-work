# Core dependencies (always required)
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
jinja2>=3.1.0,<4.0.0
pydantic>=2.6.0,<3.0.0
requests>=2.31.0,<3.0.0

# Direct Anthropic API (optional - only needed for the 'anthropic' backend)
anthropic>=0.40.0,<1.0.0

# Claude Code SDK (optional - enables the 'claude_code' backend that uses
# your existing Claude Code login. Requires the `claude` CLI on PATH.)
claude-agent-sdk>=0.0.14

# Testing (dev only)
pytest>=8.0.0
